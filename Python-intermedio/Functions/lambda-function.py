#<<---Lambda Fuctions--->>#

#<<---Normal Function--->>#
def normalFunction(x, y):
    result = x + y
    return result

#<<---Lambda Function--->>#
lambdaFunc = lambda x, y: x + y

"""
In this example we can see that the lambda functions arrive to 
the same result as a normal function, the Lambda function gets 
the input parameters before the ":", after these data we put 
the values to use with the Lambda function and to return a certain 
value. These functions can be only defined with a variable, 
'cause without it, there will be no destiny for the result that 
the function is returning.
"""